from: "cephfs:vztmpl/debian-12-standard_12.7-1_amd64.tar.zst"

metadata:
  description: "Proxer v0.2.1 test container"
  version: "1.0"

setup:
  - run: "apt update && apt install -y curl"
  - run: "echo 'Proxer test successful'"

resources:
  cores: 1.0
  memory: 512